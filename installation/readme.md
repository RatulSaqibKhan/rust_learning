# Rust Installation in Linux
- Install using this command
  ```
  curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
  ```
- For Ununtu
  ```
  sudo apt install build-essential
  ```
- Check for rust installed version
  ```
  rustc --version
  ```